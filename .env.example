# Local Vite proxy → Laravel (dev only)
VITE_API_PROXY=http://127.0.0.1:8000

# Production / staging API origin (no trailing slash).
# Empty = same-origin (only if SPA is served from Laravel).
#
# TEST:  https://enot-api.qpanel-erp.online
# PROD:  https://enot-24.com.ua   ← change when going live (or your prod API host)
VITE_API_URL=
